# This app is a pure frontend: it holds no credential. Everything here reaches
# the browser, which is why the admin key is gone — the auth server now checks
# credentials itself at /oauth/interactions/:id/login.
NEXT_PUBLIC_AUTH_SERVER_URL=http://localhost:3000
