PORT=3000

MONGO_URI=mongodb://localhost:27017
MONGO_DATABASE=oauth

ADMIN_API_KEY=changeme-admin-key

OIDC_ISSUER=http://localhost:3000

# One per identity provider in IDENTITY_PROVIDERS: where it signs people in.
# The `idp` parameter of /oauth/authorize picks which one.
LOCAL_IDP_LOGIN_URL=http://localhost:3003/login
