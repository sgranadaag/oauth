PORT=3000

MONGO_URI=mongodb://localhost:27017
MONGO_DATABASE=oauth

ADMIN_API_KEY=db2b3d21-3bf1-47f2-b289-b045a923dbc4

OIDC_ISSUER=http://localhost:3000

# Where the provider signs people in: the login app auth-server redirects to.
LOGIN_APP_URL=http://localhost:3003/login

# The browser origins allowed to call this server: both front ends are pure
# frontends now, so every token, revoke and interaction call is cross-origin.
CORS_ORIGINS=http://localhost:3001,http://localhost:3003
